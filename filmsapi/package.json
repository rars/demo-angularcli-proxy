{
  "name": "filmsapi",
  "version": "1.0.0",
  "description": "REST API",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Richard Russell",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
